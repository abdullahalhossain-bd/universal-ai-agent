# Leave blank to use Vite's dev proxy (see vite.config.js) which
# forwards /v1/* to VITE_API_PROXY_TARGET (defaults to
# http://localhost:8000). For a production build, point this at your
# deployed FastAPI origin, e.g. https://api.yourapp.com
VITE_API_BASE_URL=
